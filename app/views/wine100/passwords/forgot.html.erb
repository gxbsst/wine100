<div class="row-fluid">

	<%= form_for(@user, :url => forgot_wine100_passwords_path) do |f| %>

	<div class="control-group">
		<%= f.label :email, "#{t('email')}: " %>
		<div class="controls">
			<%= f.email_field :email, :placeholder => '请输入您的注册邮箱', :class => 'block box span6',  :required => true %>
		</div>
	</div>

	<%= f.submit "找回密码",  :class => 'btn btn-large btn-primary' %>
	<% end %>
	
</div>
