<div class="row-fluid">
	<%= render '/wine100/accounts/sidebar' %>
	<div class="span9">
		<div class="alert">
		  <button type="button" class="close" data-dismiss="alert">&times;</button>
		  <strong>注意! </strong> 修改点击编辑，取消参赛点击删除.
		</div>

		<div class="page-header">
					<h2>上传参赛酒</h2>
		</div>

		<%= form_for @wine, :url => wine100_wine_path(@wine, :step => params[:step]), :class => 'form-signin' do |f| %>
		<%= render 'form', :f => f %>
		<% end -%>
	</div><!--/span-->
</div>